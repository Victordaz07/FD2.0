import React from "react";
import { View, Text } from "react-native";

export function Tabs(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Tabs (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TabsList(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TabsList (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TabsTrigger(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TabsTrigger (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TabsContent(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TabsContent (Native stub)</Text>
      {props?.children}
    </View>
  );
}
