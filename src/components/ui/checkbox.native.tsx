import React from "react";
import { View, Text } from "react-native";

export function Checkbox(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Checkbox (Native stub)</Text>
      {props?.children}
    </View>
  );
}
