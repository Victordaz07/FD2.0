import React from "react";
import { View, Text } from "react-native";

export function Dialog(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Dialog (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogClose(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogClose (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogContent(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogContent (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogDescription(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogDescription (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogFooter(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogFooter (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogHeader(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogHeader (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogOverlay(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogOverlay (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogPortal(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogPortal (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogTitle(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogTitle (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function DialogTrigger(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>DialogTrigger (Native stub)</Text>
      {props?.children}
    </View>
  );
}
