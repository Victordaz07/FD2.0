import React from "react";
import { View, Text } from "react-native";

export function Slider(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Slider (Native stub)</Text>
      {props?.children}
    </View>
  );
}
