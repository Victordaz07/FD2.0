import React from "react";
import { View, Text } from "react-native";

export function Accordion(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Accordion (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function AccordionItem(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>AccordionItem (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function AccordionTrigger(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>AccordionTrigger (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function AccordionContent(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>AccordionContent (Native stub)</Text>
      {props?.children}
    </View>
  );
}
