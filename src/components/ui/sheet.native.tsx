import React from "react";
import { View, Text } from "react-native";

export function Sheet(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Sheet (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function SheetTrigger(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>SheetTrigger (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function SheetClose(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>SheetClose (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function SheetContent(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>SheetContent (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function SheetHeader(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>SheetHeader (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function SheetFooter(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>SheetFooter (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function SheetTitle(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>SheetTitle (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function SheetDescription(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>SheetDescription (Native stub)</Text>
      {props?.children}
    </View>
  );
}
