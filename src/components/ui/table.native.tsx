import React from "react";
import { View, Text } from "react-native";

export function Table(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Table (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TableHeader(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TableHeader (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TableBody(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TableBody (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TableFooter(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TableFooter (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TableHead(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TableHead (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TableRow(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TableRow (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TableCell(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TableCell (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function TableCaption(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>TableCaption (Native stub)</Text>
      {props?.children}
    </View>
  );
}
