import React from "react";
import { View, Text } from "react-native";

export function Command(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Command (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function CommandDialog(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>CommandDialog (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function CommandInput(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>CommandInput (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function CommandList(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>CommandList (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function CommandEmpty(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>CommandEmpty (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function CommandGroup(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>CommandGroup (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function CommandItem(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>CommandItem (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function CommandShortcut(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>CommandShortcut (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function CommandSeparator(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>CommandSeparator (Native stub)</Text>
      {props?.children}
    </View>
  );
}
