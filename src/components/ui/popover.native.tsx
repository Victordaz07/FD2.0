import React from "react";
import { View, Text } from "react-native";

export function Popover(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Popover (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PopoverTrigger(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PopoverTrigger (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PopoverContent(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PopoverContent (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PopoverAnchor(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PopoverAnchor (Native stub)</Text>
      {props?.children}
    </View>
  );
}
