import React from "react";
import { View, Text } from "react-native";

export function AspectRatio(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>AspectRatio (Native stub)</Text>
      {props?.children}
    </View>
  );
}
