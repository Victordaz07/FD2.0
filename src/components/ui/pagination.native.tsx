import React from "react";
import { View, Text } from "react-native";

export function Pagination(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Pagination (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PaginationContent(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PaginationContent (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PaginationLink(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PaginationLink (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PaginationItem(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PaginationItem (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PaginationPrevious(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PaginationPrevious (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PaginationNext(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PaginationNext (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function PaginationEllipsis(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>PaginationEllipsis (Native stub)</Text>
      {props?.children}
    </View>
  );
}
