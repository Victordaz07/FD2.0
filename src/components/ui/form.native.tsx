import React from "react";
import { View, Text } from "react-native";

export function useFormField() {
  return {};
}

export function Form(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>Form (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function FormItem(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>FormItem (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function FormLabel(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>FormLabel (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function FormControl(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>FormControl (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function FormDescription(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>FormDescription (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function FormMessage(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>FormMessage (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function FormField(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>FormField (Native stub)</Text>
      {props?.children}
    </View>
  );
}
