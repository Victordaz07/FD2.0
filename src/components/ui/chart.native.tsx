import React from "react";
import { View, Text } from "react-native";

export function ChartContainer(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>ChartContainer (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function ChartTooltip(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>ChartTooltip (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function ChartTooltipContent(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>ChartTooltipContent (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function ChartLegend(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>ChartLegend (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function ChartLegendContent(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>ChartLegendContent (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function ChartStyle(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>ChartStyle (Native stub)</Text>
      {props?.children}
    </View>
  );
}
