import React from "react";
import { View, Text } from "react-native";

export function InputOTP(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>InputOTP (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function InputOTPGroup(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>InputOTPGroup (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function InputOTPSlot(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>InputOTPSlot (Native stub)</Text>
      {props?.children}
    </View>
  );
}

export function InputOTPSeparator(props: any) {
  return (
    <View style={{ padding: 8 }}>
      <Text>InputOTPSeparator (Native stub)</Text>
      {props?.children}
    </View>
  );
}
